# Generators

This folder will contain code that converts a report schema into generated report artifacts.

Planned outputs:

- `index.html`
- `hc-report.css`
- `script.js`
- optional support files for sandbox and datasource packaging

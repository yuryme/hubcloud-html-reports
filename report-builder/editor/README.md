# Editor

This folder will contain the local browser UI for editing `report.schema.json`.

Planned responsibilities:

- create a new schema from a template,
- edit report metadata and layout,
- configure filters, tables, and datasources,
- preview the current schema JSON,
- trigger local generation.

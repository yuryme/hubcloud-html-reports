# Collaboration Protocol

## Scope
This protocol defines how we work on HubCloud report updates in this repository.

## Change Approval Policy (Global, Mandatory)
- Before any file changes, agent must first describe planned edits: what files, what will change, and why.
- Agent must receive explicit user approval (`да`, `подтверждаю`, `ok to proceed`) before editing files.
- Without explicit approval, agent may only read files and run non-mutating diagnostics.
- If this rule is violated, agent must stop, acknowledge violation, and return to approval step before further edits.

## Work Format
1. Run encoding gate first.
2. Implement and test in sandbox.
3. Run preflight checks.
4. Prepare HubCloud tab payloads (`HTML`, `Styles`, `Scripts`).
5. Transfer to HubCloud and validate.

## Commit Policy (Mandatory)
- Commits are allowed only after explicit user permission in the current dialogue turn.
- If explicit permission is absent, changes stay uncommitted.
- Explicit examples:
  - `закомить`
  - `commit`
  - `сделай коммит`
- Non-explicit feedback (for example, `ок`, `продолжаем`, `норм`) is not commit permission.

## HubCloud Tab Mapping
- `index.html` -> `HTML`
- `hc-report.css` -> `Styles`
- `script.js` -> `Scripts`
- Upload order (mandatory): `Scripts` -> `HTML` -> `Styles` -> `Ctrl+F5`.

## Mandatory Local Checks
- `node tools/hc-encoding-gate.js`
- `node --check script.js`
- `node tools/hubcloud-preflight.js`
- `node tools/hc-transfer-check.js`

## Safe Editing Rule
- Do not create or rewrite Russian-text files through shell-dependent encoding paths.
- Prefer `apply_patch` or UTF-8 writes in Node tools.
- HubCloud DS rule (mandatory): the final executable query block must NOT end with `;`.
- `;` is allowed only for intermediate temp-table definitions (`... as ИмяТаблицы;`).
- User provides only `DS.txt` (main query) and `DS_FILTERS.txt` (filter queries).
- `DS_CATALOG.md` is generated by system tools automatically.
- HubCloud DSL type rule (mandatory): numeric IDs in filters must be unquoted: `склад (1)`, `группа (4)`. Quoted numeric literals like `('1')` are forbidden.
- In `script.js`, `formatDslLiteral` must keep numeric values as numeric literals (regex: `/^-?\d+(?:\.\d+)?$/`).
- Before transfer, verify final built DS expression has no quoted numeric filter literals.

## Parameterization Rule (Mandatory)
- Parameter markers are only two symbols: `$` and `&`.
- `$...` means external/runtime parameter from HubCloud/UI context (for example `$h.date`, `$warehouse`, query parameters).
- `&...` means placeholder inside DS text that must be replaced before `/api/v1/datasource/execute/`.
- Unresolved `&...` placeholders are blocking error: report must not send request until placeholders are resolved.
- Mandatory parameter contract for each report:
  - source parameter (`$...` or UI control),
  - internal variable in `script.js`,
  - DS placeholder (`&...`) or direct template interpolation,
  - final DSL type (`number` without quotes, `string` with quotes, `datetime` boundary format).

## Delivery Note
Every handoff should explicitly mention:
- what files were changed,
- whether commit was made or not made,
- exact files to copy into HubCloud tabs.
